# Bounded fixed-clock conditional residual, next development correction

This follows clocked_prior_pair30, whose temporal branch had plausible pooled RMS/velocity but lost to its matched trained-static comparator and generated negative raw coefficients. Both calibration199 and regression353 have been consumed; they remain development. No fresh-test claim is made.

Two coupled structural corrections are fixed before this run. First, use logit(clamp(raw,1e-4,1-1e-4)) for training trajectories, subtract a single observed training-clip logit mean, retain real fixed32/hop16 shapes. Ridge(alpha10) predicts the clip logit mean from frozen global65 and independent neutral anchor logits. Decode by overlap-add in logit coordinates followed by sigmoid. Source clipping is measured explicitly; boundedness alone does not certify motion quality.

Second, train a static-acoustic shape prior30epochs using exact categorical energy score and freeze it. Train a separate acoustic correction30epochs with logits = frozen_static_logits + tanh(correction(real_audio)) - tanh(correction(static_audio)). Real and static inputs share globals. The correction starts exactly zero, cancels exactly for temporally static input, is bounded[-2,2], and cannot improve its static baseline through a global-only bypass. The frozen static network gets no gradient. Train-only clip-balanced statistics, dictionaries, equilibrium, AdamW3e-4/batch128/decay.01/gradnorm1 and seed20260918 remain fixed.

Use the same1053/199/353 membership,8 draws, fixed clock, no motion history, same feature streams as the first paired experiment. On calibration199 alone choose residual scale among0,.25,.5,1 by lowest centered fair ES subject to rawES/variogram/covariance no worse>5% than static; zero is an explicit fallback, never a timing success. Evaluate the chosen scale on consumed353 with trained static, static/reversed/mismatched audio controls. Use prior clocked timing and naturalness gates without loosening; scale0 cannot pass. Report all scale attempts and low-activity/speaker diagnostics.

This is a bounded follow-up based on two failures, not a hyperparameter search for a desired353 result. Preserve previous outputs; do not replace face defaults without timing, naturalness and full-face verification. Static image posture/identity/global emotion still need checking after eventual integration.
