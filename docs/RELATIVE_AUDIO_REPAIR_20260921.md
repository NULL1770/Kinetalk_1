# 片内声学时序与全局表达保留修复

2026-09-21。此次实施的是有证据的设计修复与新训练，不是动态已成功。

## 已定位的问题

1. 旧独立状态网络在64条固定TRAIN探针的相关0.9897，完整446开发相关0.0777；10到100轮开发均未优于static。旧分支接受1540维特征、全局情感和身份码，容量和条件可能促进身份/内容记忆。已核对归一化、stride16投影、通道映射未发现直接不一致。不能仅据此认定具体记忆途径或音频不可预测。
2. 完整开发集四种交叉替换诊断：原输出内部情感65.25%，新均值+新时序47.98%，**原均值+新时序65.02%**，新均值+原时序48.21%，原均值静态65.25%。这支持均值替换是当前内部情感下降的主要原因；评价器在GT上只有53.81%，不是独立感知认证。

## 本轮实现

- 冻结Stage4的身份/全局情感/口型。眉眼保留同噪声原输出的片内均值，替换中心化时变项；非upper43逐值保留。没有新均值网络。
- 新 `RelativeAudioTiming` 不接收身份码、全局码、query标签或query动作。音频按片内去均值；基频只在有声帧求参考均值；嵌入经TRAIN拟合24维PCA，加4维韵律及差分，再接48宽、3层小TCN。锁定特征dropout、AdamW权重衰减减少记忆风险。保留native帧mask和stride16慢状态。
- 静态对照在新分支中严格为零状态，保留相同原均值；反序只重排预测状态，不倒放原始波形。静态是此模型的嵌套零时变模型，无可训练均值，因此不另训无效参数。
- 选择阶段只从TRAIN划分出不重叠身份和句子，拟合音频统计/PCA且训练至60轮，按内部留出误差选择epoch；之后重新初始化，在全4098条TRAIN上重拟合统计和训练。446开发不选epoch、不选缩幅系数。
- 开发评估3种子，输出ARKit-MBE/LBE/FDD、完整静态/反序状态误差和时序ES/variogram、口型保护、非upper不变、均值不变及内部情感。AV、独立情感、视觉仍待实际检查。

## 验证和启动

- 本地3项测试通过：片内常量偏移/补零/无效payload不变性；零均值、通道范围和反序；小样本时间函数可学习性。
- GPU smoke完成全流程，42.88秒；只作功能检查，不宣称质量通过。
- 2026-09-21 01:42:40 CST启动正式独立进程PID2928。
- 远端：`/root/autodl-tmp/kinetalk_relative_20260921/run60`；日志位于上一级`run60.log`。
- 初步估计完整选择、全量重训和三种子评估10–25分钟，取决于内部选择的轮次与实测速度；不是完成保证。
- 此轮先执行确定性时序修复和完整验收，尚未自动接入随机残差阶段。未通过不得标记成功或换掉默认模型；随机残差需要兼容新的固定均值路径和目标，不能直接复用旧绝对均值模型的checkpoint。

源码在独立远端副本执行，旧实验权重、数据、结果保留。之前中断的FaceDiffuser评估已完成，恢复期间训练checkpoint哈希不变。

## 确定性修复终点与条件先验续训

run60于223.89秒完成。内部身份/句子选择在60轮范围内选中epoch1，之后在全部4098条TRAIN上重新训练1轮；不是漏训，较晚epoch内部留出更差。最终446开发：状态MSE0.0225897（static0.0226768），相关0.07294，R²0.00384；static差的95%CI跨零，不能称时序成功。口型、非upper43、均值、MBE及内部情感保护全通过；seed42内部情感恢复65.25%。原确定性验收失败完整保留。

为检验条件分布而非继续强行逐帧回归，新增`train_relative_motion_prior.py`：冻结上述预测器与Stage4；训练九维零均值流。目标是中心化GT减去冻结音频状态，不读取推理目标；合成时使用原Stage4均值。局部条件只有同一组相对声学24+4维及四维状态，保留冻结全局情感/身份作为分布条件，内容h0置零并保留固定native clock，减少绕过声学瓶颈的路径。小型96宽/2层DiT；零均值运动先验使用相关噪声、flow matching及每8批的双样本proper energy score。不通过手调输出幅度刷指标。

这属于公开记录的**随机建模探索续训**，不表示旧确定性门槛通过，也不将其改成成功。最终仍要求真实音频对独立训练static、同模型static和reverse在centered ES/variogram上均通过句簇配对检查，同时检查MBE、口部、均值和内部情感。动态视觉、独立AV/情感始终另算。

新增先验2项本地测试通过；完整GPU smoke44.72秒通过双臂训练和评分。初次smoke发现评估trim与固定noise维度不一致，已改为评估维持共同native存储时钟并重跑通过。初次失败结果保留，未影响已完成run60。

2026-09-21 01:52:34 CST正式启动PID3871，输出`/root/autodl-tmp/kinetalk_relative_20260921/prior40`。自动音频40轮→完整评估→独立static40轮→完整评估→配对验收。支持按同协议--resume恢复last；每轮权重和源码/数据协议绑定，默认模型不自动替换。启动时磁盘剩余899MB，正式模型较小，曲线原dtype持久化。不删除数据或旧结果。

启动后确认：audio第1/40轮完成，每轮覆盖4098条，首轮18.44秒，loss0.56770；GPU处于工作状态。按实测估计两臂80轮加完整评估约30–45分钟。17项相关本地测试通过。训练loss下降仅说明优化运行，不是效果验收。

## 2026-09-21 12:46 CST 最终复查

`prior40` 的 audio 与 static 两条40轮分支均已完成，远端状态为 `complete`，但 `quantitative_passed=false`；默认模型未替换，当前没有运行中的训练进程。磁盘仅剩约462MB，因此本轮未继续追加实验。

- audio先验相对独立训练static：centered energy score差值为−0.00123（95% CI全为负，通过），但variogram差值为+0.000243（95% CI全为正，失败）。
- audio先验相对同模型static：centered energy score差值为+0.000275（失败），variogram差值为−0.00505（通过）。
- 两个指标没有在两个对照下同时改善，不能把这轮解释为音频已经预测出可靠眉眼时序。audio full的centered ES约0.11528，几乎等于同臂static约0.11500；variogram约0.03072，改善主要体现在局部变化分布而非可确认的音频对齐时序。
- 口部保护、非upper43逐值不变、片内均值不变、MBE门控和内部情感保护均通过；AV、独立情感和视频自然度仍为pending。这些通过项不能替代动态成功。

结合前一阶段的开发集结果（真实音频状态MSE 0.029865，高于static 0.022677；相关0.07766），当前瓶颈仍是音频到状态的泛化，而不是训练进程未跑完。下一轮应先修复状态监督/划分与可预测时钟，再重新做先验；不能继续靠增加先验轮数或手调幅度把本轮标成成功。
