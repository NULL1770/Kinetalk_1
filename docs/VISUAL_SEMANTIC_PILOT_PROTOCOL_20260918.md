# 当晚视觉语义条件小试：固定执行协议

2026-09-18 22:00 CST。用户要求尽快当晚拿到实际实验。此轮为192训练/64留句内部诊断，不是完整数据正式训练或发表验收；冻结上游已经见过历史来源，不冒充全系统未见泛化。

## 数据与新监督

从已锁定1053 fit元数据中，按句子SHA排序固定8句留出，各人物×情感轮转选192训练、64留句。选择先于语义或动作数值读取，四情感，不读取405或封存test。清单在`artifacts/visual_semantics_20260918/selection.json`，源视频SHA已保存。

冻结官方EmotiEffLib `enet_b0_8_va_mtl.onnx`同时输出8类logits及VA；模型、官方预处理、训练notebook的blob与冻结commit核对。实际VA是回归头8/9列，类别分布是前8列softmax，不用类别人工映射VA。SCRFD检测矩形脸框，RGB224 ImageNet归一。固定原25Hz视频每5帧提取，不外推尾部，不跨检测缺失或native无效段插值。原始VA和截到[-1,1]的版本均保存。

这是未校准视觉伪标签。前128训练片类别均值预测准确率59.4%，不当作心理真值；首8训练片框平移2%引起明显读出变化，亮度3%影响较小。QA只诊断，未据此换片/调框/选标签，未用holdout调整教师。

## 学生与六个生成分支

音频学生：现1540维输入取768:1540（emotion2vec中层与韵律），native有效run内5帧平均，fit-only标准化/PCA64，再相邻三窗拼接，ridge固定alpha100。训练用三句折OOF预测，所有预处理逐折重拟；holdout用全192拟合模型。static是单独拟合的整段音频均值条件，绝不读取目标语义均值。音频逆序只在各有效run内处理。

六分支为VA或posterior分别接视觉oracle、audio预测、static audio。全部语义以同5帧窗口均值与块展开表示，posterior/VA补零至相同8维入口；各自fit-only尺度。六臂共享初始化、样本顺序、噪声/时间随机流、优化设置与预算。不是只在同一权重上替换来源；各分支独立训练，另外固定noise做各自语义逆序干预。

新SemanticUpperFlow为64隐藏、4膨胀时序块，联合生成9个眉/表情眼系数的归一logit坐标，无旧几何lift/P-Q和GT历史。单一FM目标；target logit先限制到[.001,.999]，报告这是训练坐标处理；最终sigmoid有界。只替换upper9，其他43通道与冻结run12基线相同。当前感受野31帧，不称长序列模型已解决。

每臂30epoch、batch16、AdamW lr3e-4/weight_decay.01、gradient clip1；192例每epoch12更新，即每臂360步。固定seed20260918，固定终点不挑epoch。可恢复状态每epoch保存；失败必须报告实际退出码，不自动重启为新实验。

生成mask只来自音频native valid；训练损失/比较评分共用有视觉监督的子集。音频分支在缺视觉标签的native有效帧仍真实生成，不借查询检测mask控制随机数或长度。只有视觉oracle在标签缺失处用自身已观测均值补条件；不得称oracle为部署方式。

## 当晚交付与判断

固定采样42/123/2026/77；报告64留句与4个元数据固定训练例的joint/group fair ES、variogram、幅度/速度/越界，以及学生raw/centered误差。两层分别判断，不能只报分类或幅度提升。

预览固定取holdout各情感clip_id字典序第一，恰为M003的neutral_L1_018、angry_L1_012、happy_L1_007、sad_L1_012，不因结果换人换片。六格：目标upper+基座43、冻结基座、VA oracle/audio、posterior oracle/audio；static给全长曲线。首批视频最多96帧并明确标记截断，seed42，保留全长预测和其他seed。其他43数值保护不替代新的口型、身份与情感感知认证。

目标是今晚拿到真实可复查结果，不能保证效果必然通过。若拟合/留句失败，报告具体差异；不临时放大动作或挑seed制造“成功”。上一状态监督已降级诊断，不再要求其先放行本次连续轨迹试验。
