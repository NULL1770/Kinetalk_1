# Low-capacity prosody correction after exact-window diagnosis

The completed high-dimensional bounded residual overfits: at scale1 the first64 fitting clips improve categorical window ES by .13765 (sentence CI positive), while calibration199 worsens by .01038 (95% CI [-.01769,-.00117]). The failure already exists before overlap-add. Merely changing the renderer or extending the same training is not justified.

Freeze the completed clocked_residual30 static prior, its dictionary, equilibrium and all normalization. Add only a small prosody controller: raw feature channels1536:1540 (log pitch, log RMS, periodicity, voicing), subtract the ORIGINAL query acoustic clip mean and divide by frozen train std. Eight ordered native bins over32 frames produce32 inputs, linear32->16, SiLU, linear16->K128. Residual logits are tanh(head(descriptor))-tanh(head(zeros)), so a static signal cancels exactly. No global or motion input enters this correction; existing full static-audio/global conditions stay with the frozen base.

Use unchanged1053 fitting/199 calibration/353 consumed development membership. Train30epochs, AdamW3e-4, weight decay.01,batch128,clip-balanced single categorical energy score,grad norm1,seed20260918. Corrected coordinates, fixed32/hop16 clock, sigmoid decoder and8 draw evaluation remain unchanged. Select scale0/.25/.5/1 only on199 with the same rawES/variogram/covariance guards, then report353 real/static/reverse/mismatch and unchanged prior quality/timing gates. Static base always receives original clip acoustics under donor intervention. Scale0 explicitly fails timing; no threshold weakening or default replacement.

This tests a specific overfitting hypothesis: a small rhythmic control may generalize better than a1540-feature temporal head. It may fail; success is not assumed. This experiment introduces no labels, extra data or new test access. Any full-face display is a diagnostic, not a certification of identity/emotion/lips or publication readiness.
