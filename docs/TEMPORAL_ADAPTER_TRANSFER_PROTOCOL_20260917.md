# Restricted temporal-feature adaptation: locked incremental transfer experiment

Locked before smoke/formal training, 2026-09-17. Existing model/defaults/data remain intact. This is an engineering experiment, not a claim of novel architecture or successful dynamics.

## Question and prerequisite evidence

Audio_prefix12 full local unfreezing increased fit correlation much more than development correlation. In the saved seed42 development diagnostic, time-varying local features improve correlation on 67.2%/66.4% of brow/eye clips, but improve centered MSE on only29.6%/35.6%. Full conditions raise amplitude from1.067/1.070 to1.331/1.242 relative to reference. Thus local time supplies weak useful timing and excess variability; removing time is not accepted as success merely for reducing MSE.

Display [0,1] clamping preserves99.26%/99.98% of raw brow/eye centered RMS and93.51%/99.99% after audio-mean composition. It discards over half of brow RMS in2/405 clips, eyes0/405. Saturation remains a domain issue but is not the main aggregate explanation for missing dynamics. Full diagnostic is bound by audio_prefix12/local_temporal_transfer_audit/complete.json. No gain, smoothing, lag fitting, or output clipping is introduced to training or numerical scoring.

## Membership, initialization, and budget

Use the existing2315 fit and405 repeatedly used internal development members. Reproduce scripts.probe_dynamic_predictability.sentence_split with seed2026091708 and fraction.2 and assert exact agreement with repair12/scale_diagnosis/report.json. This gives1707 updates-eligible clips/54sentences and608 clips/13sentences held out from NEW updates. No motion-based filtering or sample selection. The inherited upper/local/scales/normalization have already seen the complete fit pool; this is incremental adaptation transfer, not clean unseen-encoder generalization. Development remains a reference, not a checkpoint/rank/epoch selection set; sealed test stays unread.

Three arms frozen_local/rank8_adapter/full_local start from the same context12/chunk_teacher upper and original history12/no_history local, not the latest fully adapted endpoint. All train upper; frozen_local leaves local frozen, full_local unfreezes onlyinput/blocks/local_head, rank8_adapter freezes local and updates the new1024parameter module. Its unused copy is identically initialized and frozen in the other arms. All use seed89, exact paired clip order, noise[B,96,9], time[B], batch16, AdamW1e-4/weight_decay1e-5, existing clip1,12epochs/1284updates. One training seed, three fixed inference seeds42/123/2026. No result-driven extension, best-seed choice, conditional full-fit launch, or default replacement. The old full2315 adaptation is a historical reference, not a matched capacity control.

## Feature interface

l0=original_local(audio); d=center_valid(l0); r=W_up SiLU(W_down d); l=l0+center_valid(r). Bias-free64→8→64, rank8 fixed. W_up zero, isolated initialization seed89 that preserves ambient CPU/CUDA random streams. Step0 feature/output replay must match source; first-step up gradient and second-step down gradient must be nonzero. Both centers use full native96-frame valid masks; invalid frames copy source, padding is excluded. This is offline full-clip conditioning, not causal streaming or a new long-context temporal architecture. Mean preservation applies to64D local FEATURES, not generated face means. It cannot restore information discarded by the original local encoder.

Keep existing unknown-only flow-matching, eight clean strictly-past known motion tokens and16unknown tokens; clamp known values each Euler step,12Euler steps. GT past only training and separately labeled oracle; deployment generated past. Preserve original residual coordinates (GTupper-static_upper)/fixed_source_scales. No new loss, teacher condition, global head, state path, origin projection, output gain, or scheduled sampling. Source global emotion/motion-to-audio training is inherited; this stage does not perform new global distillation. Original system/audio/identity/local frozen hashes and unused local heads are checked.

## Evaluation and storage

Before updates, score complete608 holdout with source upper and original cached local, using three seeds plus local-onlystatic/reverse seed42. At each fixed endpoint evaluate all608 with exact same membership/noise, plus405 development via existing raw/DC evaluator and all historical deployment/history/oracle controls. Main transfer decoder receives only valid,h0,audio global/intensity,local features,static upper and independent identity code; motion/label/targetmean never passed. Upper-only transfer output is clearly diagnostic and makes no43-channel/mouth claims. Fixed128 metadata-selected adaptation-fit clips add reconstruction diagnosis; their selection differs from historical128 and is explicitly stored. Feature changes per clip/sentence are saved for fit/holdout/development.

Report raw and DC independently. DC=audio static_upper+raw prediction minus its valid-frame mean, offline after full rollout, never history feedback; no DC oracle continuation. For transfer retain perclip and pooled raw/centered MSE/correlation/amplitude, native velocity, seams,3sample fairES/VS and local-onlystatic/reverse. Compare each endpoint to same source step0 and the matched other arms. Require broad evidence; higher corr with worse eye displacement or distribution scores is mixed, not success.

Full development evaluation first constructs full52 coefficient outputs and asserts43non-upper channels+invalid frames bitexact to the same seed-specific frozen base. For storage only, save upper9 predictions plus native metadata and the existing base path/SHA; reconstruct and verify bitexact BEFORE saving. DC andraw compact archives remain separate. Transfer stores upper9-only raw samples and audio means, DC reconstructed; invalid entries are zero placeholders never scored. This saves duplicate protected-channel storage on the constrained server, without changing inference or scoring.

Fresh independent output, last optimizer/RNG checkpoint and final checkpoint per arm, compact samples and JSON reports. Require1.2GB free at start and512MB while training, and adequate room before evaluation. Do not delete or overwrite previous experiments. Smoke uses32update/16transfer/32dev/8fit clips, oneepoch(twoupdates)perarm, all evaluation modes/seeds and reconstruction checks. Only after tests/smoke/gradients/matched controls pass start formal12epochs. Record actual first-epoch/evaluation time and revise time estimate from measurements.

No claim of identity, lipsync, emotional naturalness, perceptual motion, test generalization, or publication readiness follows from preservation or the nonindependent teacher readout. Final visuals use the historical fixed nine clips and three videos, no queryGT-history in deployment panels. If restricted adaptation cannot improve transfer without renewed velocity/distribution harm, do not increase capacity or epochs to chase the same dev set.

Pre-update implementation correction (2026-09-18): initial full608 evaluation now recomputes specialized local features in identical batch geometry to endpoint evaluation; the inherited full-forward cache is not used as the step0 feature source. First training batch compares same-forward frozen original local to each initialized arm bitexact, and records old-cache max/RMS discrepancy separately. The first formal attempt stopped before its first optimizer update due to old-cache allclose rejection; its output is retained. This correction changes no data membership, training objective, or fixed budget. Adapter mean preservation likewise compares the same forward before/after adapter, with unchanged2e-6 tolerance.
