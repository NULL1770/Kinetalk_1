# Joint empirical motion-prior experiment, 2026-09-18

The authorized change is joint upper-face short-trajectory support followed by limited audio control. This pilot is an empirical motion dictionary, not a claim of learned semantic tokens or a novel neural motion latent. Existing identity, mouth and global emotion weights remain frozen. The global audio source retains its historical motion-emotion distillation.

## Membership and frozen inputs

Reuse only the 2,315 historical training queries through the audited native loader. Within the previous 1,605 fit cell, sort sentences by SHA256(`joint_motion_20260918:sentence:` + sentence): first ten sentences are new-stage confirmation (353 clips), next ten calibration (199), remainder dictionary/local-fit (1,053). This split is fixed before trajectory access. Confirmation is only evaluated after calibration and fitting; historical 275 sentence/375 speaker/60 joint cells are descriptive development references, not new confirmation. Old upstream weights have seen these sources, so no whole-model unseen-source claim. Never index 405 or sealed test.

Extract the existing run12/audio/final.pt frozen global64, intensity1 and local64 on full native audio. Verify checkpoint/recipe/input hashes. Audio extraction API cannot receive query motion, labels or anchors. Query emotion labels are only used for strata and mismatch donor construction. Independent enrollment upper9 anchors define identity coordinates.

## Joint trajectory dictionary

Read channels [41,42,43,44,45,5,6,12,13] jointly. Extract eight-frame prefixes and 16/32/48-frame futures at stride eight inside uninterrupted valid runs. At each eligible start, a fixed clip/start hash selects one available duration. Fit only dictionary-fit clips. Coordinates are logit(clamp(raw,1e-4,1-1e-4)) minus logit(independent anchor), with clipping effects reported. Durations are control-unit lengths, not semantic event boundaries.

Fit K=128 joint clusters with equal total weight per source clip, using an eight-frame prefix and resampled 32-frame future for distance; retain one distinct real medoid per cluster with its native duration. Record all sources, source identity proportions, coordinate scales and original medoid trajectories. Nearest-query motion matching is permitted only in a separately named oracle coverage diagnostic. No query motion, mean, RMS, token or boundary enters free generation.

At deployment a token produces an entire nine-channel trajectory in target enrollment coordinates. At a continuation, an eight-frame logit correction decays to zero; later frames revert to the absolute prototype trajectory, preventing cumulative delta drift. Whole-token state persists across any renderer chunk. This bounded parameterization prevents output-domain violations by construction; zero violations alone are not evidence of motion quality. Report source clipping and temporal amplitude retention. Missing frames split runs, never compress time.

## Motion-prior selection and gates

Compare uniform-token replay, history-only prior, and global-emotion+history prior using the same dictionary, sampling keys and continuation rule. Prior logits use negative normalized squared history-prefix distance and negative normalized squared global-embedding distance. Calibration searches exactly history weights {1,4,16} and global weights {1,4,16}; history-only compares the three history weights. Select the lowest calibration joint centered fair energy score subject to pooled group RMS in [.4,2.5], joint variogram and velocity-covariance error no worse than uniform replay by >10%, and seam/within pooled RMS <=1.5. Calibration may reject all candidates; do not loosen the gates after results. Fit/calibration/confirmation metrics and every searched setting are saved.

Oracle dictionary coverage is reported separately against a constant-state oracle and input-clipped reference, at both nine-channel and four-group levels; it is not a cross-modal success gate. A motion prior must also pass the above absolute/relative motion gates on confirmation, without selecting a new setting, before further generator integration. Free generation uses eight fixed draws, all scored, not best-of-N. Report raw/centered joint trajectory fair ES, four group ES/RMS, four-lag variograms, nine-channel covariance and velocity covariance, raw means and boundaries. Use pooled energy ratios, with near-static clips separately described. Long 256/1024-frame continuation is a stability diagnostic, not new natural data. No correction reads query GT.

## Local-audio control

Only if calibration motion quality is feasible, freeze dictionary and prior, then fit a small local classifier for 30 fixed epochs (seed20260918, AdamW3e-4, decay.01, batch256, hidden64). Input is frozen local64 summarized at four future subwindows, together with global65 and the generated/teacher past descriptor. Output changes K logits by at most +/-1 through tanh, never changes trajectory values, durations or variance. Teacher targets are fixed nearest joint units; training/likelihood may use teacher prefix, while free rollout only uses generated history. Tail/gap incomplete windows excluded from teacher classification; inference remains full native length with truncation only at recording boundaries. No checkpoint selection on confirmation.

At the final checkpoint select local scale from {0,.25,.5,1} on calibration; scale0 is the same frozen global prior. Confirmation is evaluated once with that choice plus the fixed global baseline. Static-local, reverse-local and same-speaker/emotion different-sentence mismatched-local interventions retain original global context. Do not call a scale0 selection or better token CE a success. Audio gate requires real local confirmation joint centered ES paired sentence-bootstrap improvement with lower95%>0, at least1% mean improvement, rawES/variogram/covariance degradation <=5%, and motion gates; real must outperform static and same-support mismatch. Otherwise stop at a failed feasibility result, no blind generator training.

If these gates pass, integrate into the protected upper9 generation path and independently verify copied43/invalid, full-face visual quality and global emotion. Such integration is conditional and is not claimed by this stand-alone experiment. Keep original default and prior negative runs. Save source/code/protocol hashes, all selections, normalization, final weights, per-clip metrics and all confirmation draws. Recompute metrics from saved draws before delivery.
